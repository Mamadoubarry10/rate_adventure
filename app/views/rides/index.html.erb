
<div class="index-container">
  <% @rides.each do |ride| %>
     <div class="index-card">
      <h2><%= link_to ride.name, ride_path(ride) %></h2>
      <img src="<%= ride.image %>" alt="<%ride.name%>"  style="width:50%" , class="index-img">
    </div>
  <% end %>
</div>