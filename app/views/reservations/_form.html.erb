
<%= form_for @reservation do |f| %><br>
<%= f.label :ride_id %>
<%= f.collection_select :ride_id, Ride.all, :id, :name %>

<%= f.datetime_select :date_time, { ampm: true, order: [:month, :day, :year] }, { class: "form-control" } %>

<%= f.submit %>
<% end %>