
<%= form_for @reservation do |f| %><br>
<%= f.label :ride_id %>
<%= f.collection_select :ride_id, Ride.all, :id, :name %>

<%= f.datetime_select :check_out_req, { ampm: true, order: [:day, :month, :year] }, { class: "form-control" } %>

<%= f.submit %>
<% end %>